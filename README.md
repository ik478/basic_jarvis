# basic_jarvis
thhis repository is for basic installing of jarvis ai which runs based on open ai api
so it is necessary to create an open ai API key and past that key in the file name called data/Api.txt
after that you can install all the libraries which are present in requirements.txt file using CMD pip install -r requirements.txt

note : first you have to install python in your computer
